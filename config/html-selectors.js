////////////////////// THIS IS WHERE YOU SET HTML SELECTORS FOR THE CODE ///////////////////////////////////

let selector_postLinks = "._52jc"; // Selector for post links
let selector_date = "._52jc._5qc4._78cz._24u0._36xo"; // Selector for the date or time of post
let selector_noOfComments = "._1j-c"; // Selector for the number of comments
let selector_noOfShares = "._1j-c"; // Selector for the number of shares
let selector_post = "._5rgt._5nk5._5msi"; // Selector for the post content
let selector_img = "._5rgu._7dc9._27x0"; // Selector for the image of the post
let selector_name = "._4kk6"; // Selector for the name


module.exports = {
    selector_postLinks,
    selector_date,
    selector_noOfComments,
    selector_noOfShares,
    selector_post,
    selector_img,
    selector_name
}